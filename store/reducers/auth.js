const initialState = {
  token: null,
  user: {
    id: null,
    name: null,
  },
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
